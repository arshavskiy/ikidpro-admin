server {
    listen 80 default_server;
    listen [::]:80 default_server;

    server_name your_domain_or_ip;

    root /var/www/ikidpro-admin;
    index index.html;

    location /admin {
        try_files $uri $uri/ /index.html;
    }
}